package com.flex.ast.fbs

import com.flex.ast.LoadTree
import com.flex.ast.ObjectDefinitionVisitor
import com.flex.ast.ObjectDefinitionVisitorTest
import com.flex.core.PineContext
import com.flex.core.PineEngine
import com.google.flatbuffers.FlatBufferBuilder
import junit.framework.Assert.assertEquals
import org.junit.After
import org.junit.Assert
import org.junit.Before
import org.junit.Test
import java.io.IOException


class TestFlatbuffersData {
    @Before
    @Throws(Exception::class)
    fun setUp() {

    }

    @After
    @Throws(Exception::class)
    fun tearDown() {
    }

}